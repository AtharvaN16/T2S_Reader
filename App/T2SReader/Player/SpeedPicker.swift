import SwiftUI

/// Replaced with the sustainable-rate picker in Task 6.
struct SpeedPicker: View {
    var body: some View {
        Text("Speed").typeRole(.rowTitle).padding(Spacing.margin)
    }
}
